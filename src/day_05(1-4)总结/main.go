package main

func main() {
	/**
	go build 编译
		1.go build -o "xxx.exe" 修改编译出的文件名
		2.go install 约等于 npm i xxx -g，一般不用
	go语言基础文件语法
		1.文件开头需要声明包名
		2.main函数是入口函数，既无传参也无返回值
		3.main函数外声明变量只能使用var
		4.定义的变量必须使用
	变量
		var str string
		var num = 1
		flag := false（函数内使用）
	常量
		const声明
		iota每遇到一个const就会被重置为0
		const中每新增一个常量声明，iota就会加一
	流程控制
		1.if
		2.for，go语言中只有一种循环，就是for循环
		3.for循环中可使用 for range来遍历可迭代变量
	*/
}
